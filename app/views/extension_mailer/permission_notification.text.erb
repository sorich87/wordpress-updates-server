A customer wants to get automatic updates. Approve them to make their lives at least 10% easier:

<%= link_to "Approve #{@customer.email}", new_customer_url(email: @customer.email, auth_token: @user.authentication_token) %>
